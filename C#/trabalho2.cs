// dotnet new console

// undefined nao existe aqui
//      toda a variavel deve ser atribuida
//      null ainda é valido

// inferencia de tipo: usar var ao inves de string, int, etc.
//      a definição da natureza da variavel ocorre pelo outro lado da igualdade
//      essa definição ocorre no momento da atribuição
//      var x = null não funciona

/* string nome = "jaaziel", jazz = "";

nome = Strings.Caption(nome);


for (int i = 0; nome[i] != 'i'; i++)
{
    if (i == 0 || nome[i] != nome[i - 1])
    {
        jazz += nome[i];
    }
} jazz += jazz[jazz.Length - 1];

Console.WriteLine(jazz); */